 
```bash
boss="Mayo"
```
>this will create a variable in linux env

```bash
export boss
```
>this will make this variable global and allow child process to use it

```bash
echo $((arithimatic exp))
```
> to perform aritimatic expression

```bash
if [[ expression ]]; then
	output
else
	output
fi
```

```bash
while true ; 
do
```